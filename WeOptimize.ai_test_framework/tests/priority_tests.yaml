  - vars: { ticket: HFLO-30 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-31 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-32 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-33 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-34 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-35 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-36 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-37 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-38 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-39 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-40 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-41 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-42 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-43 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-44 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-45 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-46 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-47 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-48 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-49 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-50 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-51 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-52 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-53 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-54 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-55 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.

  - vars: { ticket: HFLO-56 }
    assert:
      - type: llm-rubric
        value: |
          PASS if the response clearly states exactly one priority
          from {Highest, High, Medium, Low} with no mixing or hedging.
