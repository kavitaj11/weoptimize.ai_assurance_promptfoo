# Prompts

Corpora for:
- `harmful/` – hate, self-harm, illegal requests (store responsibly; consider encoding)
- `pii/` – synthetic PII only
- `bola/` – Broken Object-Level Authorization probes
- `injection/` – prompt injection, jailbreak, ASCII smuggling
- `templates/` – parameterized prompts
